server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # กันหน้าขาว/404 ถ้ามีการเข้าตรงด้วย path ย่อยในอนาคต (แอปนี้เป็น SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
