-- ==========================================================
-- เพิ่มสิทธิ์ "เช็คชื่อแทนเพื่อนในสีเดียวกัน" ให้นักศึกษาบางคน
-- รันไฟล์นี้ใน pgAdmin Query Tool (เลือกฐานข้อมูล sports_day ก่อน)
-- ==========================================================

ALTER TABLE students
  ADD COLUMN can_checkin BOOLEAN NOT NULL DEFAULT false;

-- ค่าเริ่มต้นทุกคนคือ false (ยังไม่มีสิทธิ์)
-- แอดมินจะไปกดมอบสิทธิ์ให้ทีละคนผ่านหน้าเว็บ (หน้า "จัดการนักศึกษา") ได้เลย
