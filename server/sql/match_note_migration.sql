-- ==========================================================
-- เพิ่มช่อง "หมายเหตุผลการแข่งขัน" แบบข้อความ ให้ตาราง matches
-- รันไฟล์นี้ใน pgAdmin Query Tool (เลือกฐานข้อมูล sports_day ก่อน)
-- ==========================================================

ALTER TABLE matches ADD COLUMN note TEXT;
