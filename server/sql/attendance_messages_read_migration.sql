-- ==========================================================
-- เพิ่มการติดตามว่าอ่านข้อความแล้วหรือยัง เพื่อโชว์ตัวเลขแดง "ข้อความใหม่"
-- ที่แถบ "ประวัติของฉัน" ของนักศึกษา
-- รันไฟล์นี้ใน pgAdmin Query Tool (เลือกฐานข้อมูล sports_day ก่อน)
-- ==========================================================

ALTER TABLE attendance_messages ADD COLUMN is_read BOOLEAN NOT NULL DEFAULT FALSE;

-- ข้อความเก่าที่มีอยู่แล้วก่อนฟีเจอร์นี้ ถือว่าอ่านไปแล้วทั้งหมด ไม่ต้องขึ้นเลขแดงย้อนหลัง
UPDATE attendance_messages SET is_read = TRUE;
